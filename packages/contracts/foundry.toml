[profile.default]
src      = "src"
out      = "out"
libs     = ["lib"]
test     = "test"
script   = "script"
solc     = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir   = true

[fuzz]
runs = 1000
seed = "0x1"

[invariant]
runs  = 256
depth = 50

[rpc_endpoints]
robinhood_mainnet = "${ROBINHOOD_MAINNET_RPC:-https://rpc.mainnet.chain.robinhood.com}"
robinhood_testnet = "${ROBINHOOD_TESTNET_RPC:-https://rpc.testnet.chain.robinhood.com}"

[etherscan]
robinhood_mainnet = { key = "${BLOCKSCOUT_API_KEY:-verifyContract}", url = "https://robinhoodchain.blockscout.com/api" }
robinhood_testnet = { key = "${BLOCKSCOUT_API_KEY:-verifyContract}", url = "https://explorer.testnet.chain.robinhood.com/api" }
